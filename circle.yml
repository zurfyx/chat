machine:
  services:
    - docker

dependencies:
  pre:
    - docker login -e $DOCKER_EMAIL -u $DOCKER_USER -p $DOCKER_PASS

test:
  post:
    - docker-compose run web npm test

deployment:
  hub:
    branch: master
    commands:
      - docker push zurfyx/nyao